using System.Threading.Tasks;
using Shouldly;
using Xunit;

namespace {{ EntityInfo.Namespace }};

public sealed class {{ EntityInfo.Name }}DomainTests : {{ ProjectInfo.Name }}DomainTestBase<{{ ProjectInfo.Name }}DomainTestModule>
{
    public {{ EntityInfo.Name }}DomainTests()
    {
    }
}